const hero = 'Deadpool';
const actor = 'Wade Winston';

const normal = hero + ' ' + actor;
const template = `El heroe es: ${hero} y el nombre del actor es ${actor} Template String`;

console.log(normal);
console.log(template);

const html = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log(html);
